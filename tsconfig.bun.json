{
	"extends": "./tsconfig.base.json",
	"compilerOptions": {
		"composite": true,
		"types": ["bun-types"]
	},

	// Setup linting for tests later
	"include": ["tests/**/*.ts", "lint-staged.config.ts", "bunup.config.ts"]
}
